{
    "name": "API User Rights",
    "namespace": "YaleREDCap\\ApiUserRights",
    "description": "This external module allows users with User Rights permission in a project to control access to individual API methods. For example, you can allow a user to access the <code>Export Reports</code> method, but not the <code>Import Records</code> endpoint.",
    "authors": [
        {
            "name": "Andrew Poppe",
            "email": "andrew.poppe@yale.edu",
            "institution": "Yale Center for Clinical Investigation"
        }
    ],
    "system-settings": [
        {
            "key": "default-rights-csv",
            "name": "<strong>Upload a CSV file that defines the default API User Rights a user will have.</strong><br>The format of the file should match that obtained from an export of the API User Rights in a project. It should only have one row besides the header row (and no username column is required). If neither a project-specific default nor a system-level default is defined, then no API User Rights will be granted by default.<br>A template file can be downloaded <a href='https://raw.githubusercontent.com/AndrewPoppe/ApiUserRights/main/files/API_User_Rights_Default_Template.csv'>here</a>",
            "type": "file"
        }
    ],
    "project-settings": [
        {
            "key": "default-rights-csv-project",
            "name": "<strong>Upload a CSV file that defines the default API User Rights a user will have.</strong><br>The format of the file should match that obtained from an export of the API User Rights in the project. It should only have one row besides the header row (and no username column is required). If no file is defined here, then the system-level default rights will be used.<br>A template file can be downloaded <a href='https://raw.githubusercontent.com/AndrewPoppe/ApiUserRights/main/files/API_User_Rights_Default_Template.csv'>here</a>",
            "type": "file"
        }
    ],
    "links": {
        "project": [
            {
                "name": "API User Rights",
                "url": "api_user_rights",
                "icon": "fas fa-laptop-code",
                "show-header-and-footer": true
            }
        ]
    },
    "auth-ajax-actions": [
        "getApiUserRights",
        "saveApiUserRights",
        "importRightsCsv",
        "saveApiUserRightsSnapshot",
        "getSnapshotsInfo",
        "downloadSnapshot"
    ],
    "framework-version": 12,
    "compatibility": {
        "php-version-min": "7.4.0",
        "php-version-max": "",
        "redcap-version-min": "13.1.37",
        "redcap-version-max": ""
    },
    "enable-every-page-hooks-on-system-pages": true
}